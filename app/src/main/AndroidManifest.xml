<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!--
      Reading the device's own surroundings, so a new profile can start out
      describing where the phone is. All three sources are gated on location:
      a scan result and a serving cell disclose a position as plainly as a fix
      does. NEARBY_WIFI_DEVICES carries no neverForLocation flag on purpose -
      what the scan is for here is precisely the place it implies.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MockLocation"

        tools:replace="android:icon">
        <activity
            android:name="mock.location.app.ui.activities.MainActivity"
            android:theme="@style/Theme.MockLocation.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".app.ui.activities.ModuleActivity"
            android:parentActivityName=".app.ui.activities.MainActivity"
            android:theme="@style/Theme.MockLocation.NoActionBar"
            android:label="@string/select_apps_activity_title"/>

        <activity android:name=".app.ui.activities.AboutActivity"
            android:theme="@style/Theme.MockLocation.about"/>

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Advanced way of granting location" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
    </application>
</manifest>